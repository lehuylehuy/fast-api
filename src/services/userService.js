import db from  "../models/index";

let handleUserLogin = (email, password) => {
 
}
let checkUserEmail = (email) => {
  //check owr DB ton time nen bat dong bo

  return new Promise((resolve, reject) => {
    try {
      let user
    } catch (error) {
      reject(error)
    }
  })
}

modules.exports = {
  handleUserLogin
}